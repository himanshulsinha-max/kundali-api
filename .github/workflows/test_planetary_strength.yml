name: Test Planetary Strength

on:
  push:
    paths:
      - "planetary_strength.py"
      - "test_planetary_strength.py"
      - ".github/workflows/test_planetary_strength.yml"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyswisseph

      - name: Run planetary strength tests
        run: |
          python test_planetary_strength.py
